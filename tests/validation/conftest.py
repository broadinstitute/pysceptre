import json
import subprocess
from pathlib import Path

import pytest

VALIDATION_DIR = Path(__file__).parent
GROUND_TRUTH_PATH = VALIDATION_DIR / "ground_truth.json"
DUMP_SCRIPT = VALIDATION_DIR.parent.parent / "scripts" / "dump_r_ground_truth.R"


def _r_sceptre_available() -> bool:
    try:
        result = subprocess.run(
            ["Rscript", "-e", "library(sceptre)"],
            capture_output=True,
            timeout=30,
        )
        return result.returncode == 0
    except (FileNotFoundError, subprocess.TimeoutExpired):
        return False


@pytest.fixture(scope="session")
def ground_truth():
    if not GROUND_TRUTH_PATH.exists():
        if not _r_sceptre_available():
            pytest.skip("R/sceptre not available and no cached ground_truth.json present")
        subprocess.run(["Rscript", str(DUMP_SCRIPT), str(GROUND_TRUTH_PATH), "4"], check=True)
    with open(GROUND_TRUTH_PATH) as f:
        return json.load(f)


PERTURBPLAN_GROUND_TRUTH_PATH = VALIDATION_DIR / "perturbplan_ground_truth.json"
PERTURBPLAN_DUMP_SCRIPT = (
    VALIDATION_DIR.parent.parent / "scripts" / "dump_perturbplan_ground_truth.R"
)


def _r_perturbplan_available() -> bool:
    try:
        result = subprocess.run(
            ["Rscript", "-e", "library(perturbplan)"],
            capture_output=True,
            timeout=60,
        )
        return result.returncode == 0
    except (FileNotFoundError, subprocess.TimeoutExpired):
        return False


@pytest.fixture(scope="session")
def perturbplan_ground_truth():
    """PerturbPlan's own output for the analytical power port.

    Regenerated only if the JSON is missing, like `ground_truth`, so CI needs
    neither R nor perturbplan. If you change
    `scripts/dump_perturbplan_ground_truth.R`, delete the JSON or the tests
    keep validating against the stale fixture.
    """
    if not PERTURBPLAN_GROUND_TRUTH_PATH.exists():
        if not _r_perturbplan_available():
            pytest.skip(
                "R/perturbplan not available and no cached perturbplan_ground_truth.json present"
            )
        subprocess.run(
            ["Rscript", str(PERTURBPLAN_DUMP_SCRIPT), str(PERTURBPLAN_GROUND_TRUTH_PATH)],
            check=True,
        )
    with open(PERTURBPLAN_GROUND_TRUTH_PATH) as f:
        return json.load(f)


POSCOUNTS_GROUND_TRUTH_PATH = VALIDATION_DIR / "poscounts_ground_truth.json"
POSCOUNTS_DUMP_SCRIPT = VALIDATION_DIR.parent.parent / "scripts" / "dump_poscounts_ground_truth.R"


def _r_deseq2_available() -> bool:
    try:
        result = subprocess.run(
            ["Rscript", "-e", "library(DESeq2)"],
            capture_output=True,
            timeout=120,
        )
        return result.returncode == 0
    except (FileNotFoundError, subprocess.TimeoutExpired):
        return False


@pytest.fixture(scope="session")
def poscounts_ground_truth():
    """DESeq2's own poscounts size factors, for the baseline-mean helper.

    DESeq2 defines the estimator, so it is the reference rather than a second
    transcription of the same arithmetic. Regenerated only if the JSON is
    missing, so CI needs neither R nor DESeq2; delete the JSON if you change
    `scripts/dump_poscounts_ground_truth.R`.
    """
    if not POSCOUNTS_GROUND_TRUTH_PATH.exists():
        if not _r_deseq2_available():
            pytest.skip("R/DESeq2 not available and no cached poscounts_ground_truth.json present")
        subprocess.run(
            ["Rscript", str(POSCOUNTS_DUMP_SCRIPT), str(POSCOUNTS_GROUND_TRUTH_PATH)], check=True
        )
    with open(POSCOUNTS_GROUND_TRUTH_PATH) as f:
        return json.load(f)


SINGLETON_GROUND_TRUTH_PATH = VALIDATION_DIR / "singleton_ground_truth.json"
SINGLETON_DUMP_SCRIPT = VALIDATION_DIR.parent.parent / "scripts" / "dump_singleton_ground_truth.R"


@pytest.fixture(scope="session")
def singleton_ground_truth():
    """sceptre's own pair expansion under `grna_integration_strategy="singleton"`.

    Generated by calling `update_dfs_based_on_grouping_strategy` on minimal
    sceptre objects, so it is their function rather than a reading of it.
    Regenerated only if the JSON is missing; delete it if you change
    `scripts/dump_singleton_ground_truth.R`.
    """
    if not SINGLETON_GROUND_TRUTH_PATH.exists():
        if not _r_sceptre_available():
            pytest.skip("R/sceptre not available and no cached singleton_ground_truth.json")
        subprocess.run(
            ["Rscript", str(SINGLETON_DUMP_SCRIPT), str(SINGLETON_GROUND_TRUTH_PATH)], check=True
        )
    with open(SINGLETON_GROUND_TRUTH_PATH) as f:
        return json.load(f)
